import React from 'react'
import ReactDOM from 'react-dom/client'
import ProjectRoot from './ProjectRoot.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ProjectRoot />
    </React.StrictMode>,
)
